// Access the value in output

const colors = {
    red: "#ff0000",
    green: "#00ff00",
    blue : "#0000ff",
    "golden rod": '#daa520'
};

const aColor = colors["golden rod"];

console.log(aColor);

// Access the golden rod color value in output has done. (solved)